version: '3.8'

services:
  elyos-app:
    build: .
    ports:
      - "80:80"
    volumes:
      # Montage pour le développement (facultatif, retirez si prod)
      - ./src:/app/src
      - ./templates:/app/templates
      - ./static:/app/static
      - ./logs:/app/logs
    environment:
      - PYTHONUNBUFFERED=1
      - TZ=Europe/Paris
